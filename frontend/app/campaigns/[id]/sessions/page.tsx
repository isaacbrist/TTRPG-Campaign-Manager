import type { Metadata } from "next";
import SessionsPageClient from "@/components/pages/SessionsPageClient";

export const metadata: Metadata = {
  title: "Sessions",
};

export default SessionsPageClient;
